# راهنمای جامع پیاده‌سازی مدیریت دسته‌بندی‌های هزینه در فرانت‌اند

این سند قرارداد API و الزامات رابط کاربری CRUD دسته‌بندی‌های هزینه را برای فرانت‌اند تعریف می‌کند.

## محل قابلیت در رابط کاربری

- در **همان منوی «حسابداری»** یک مسیر/گزینه برای «مدیریت دسته‌بندی‌های هزینه» اضافه شود.
- این بخش باید داخل **Overview حسابداری** قابل دسترسی و مشاهده باشد؛ پیشنهاد می‌شود یک کارت یا اکشن واضح با عنوان «مدیریت دسته‌بندی‌های هزینه» در Overview قرار گیرد.
- کلیک روی این گزینه باید کاربر را به یک **صفحه مستقل** مدیریت دسته‌بندی‌ها هدایت کند؛ این قابلیت نباید صرفاً یک مودال بدون صفحه اصلی باشد.
- خود صفحه شامل عنوان، توضیح کوتاه، دکمه «افزودن دسته‌بندی» و فهرست دسته‌بندی‌ها است.
- عملیات **افزودن، ویرایش و حذف همگی باید در Modal** انجام شوند. نمایش جزئیات می‌تواند Modal/Drawer باشد، اما صفحه فهرست نباید ترک شود.

## احراز هویت و ساختار عمومی پاسخ

تمام Endpointها به JWT نیاز دارند:

```http
Authorization: Bearer <access-token>
Content-Type: application/json
```

ساختار عمومی پاسخ موفق داده‌دار:

```json
{
  "isSuccess": true,
  "message": "پیام فارسی عملیات",
  "data": {}
}
```

ساختار عمومی خطا:

```json
{
  "isSuccess": false,
  "message": "شرح خطا",
  "data": null
}
```

فرانت‌اند باید `message` برگشتی API را در Toast/Snackbar نشان دهد و صرفاً به Status Code اکتفا نکند.

## مدل داده

```ts
export interface ExpenseCategory {
  id: number;
  title: string;
  isActive: boolean;
  createdAt: string;       // UTC ISO-8601
  updatedAt: string | null; // UTC ISO-8601
}

export interface ApiResult<T> {
  isSuccess: boolean;
  message: string;
  data: T;
}
```

شناسه در Backend از نوع `long` است. تا زمانی که شناسه از محدوده امن JavaScript خارج نشده، `number` قابل استفاده است؛ در کلاینت‌هایی که تبدیل دقیق `Int64` دارند می‌توان آن را به صورت string نگهداری کرد.

## APIها

Base URL:

```text
/api/secretary/expense-categories
```

### ۱. دریافت فهرست مدیریتی

```http
GET /api/secretary/expense-categories
```

این API تمام دسته‌بندی‌های حذف‌نشده را، شامل دسته‌بندی فعال و غیرفعال، برمی‌گرداند و برای صفحه مدیریت استفاده می‌شود.

```json
{
  "isSuccess": true,
  "message": "",
  "data": [
    {
      "id": 12,
      "title": "مواد مصرفی",
      "isActive": true,
      "createdAt": "2026-08-30T12:00:00Z",
      "updatedAt": null
    }
  ]
}
```

> Endpoint قدیمی `GET /api/secretary/account/expense-categories` فقط دسته‌بندی‌های فعال را برای انتخاب در فرم ثبت تراکنش برمی‌گرداند. برای صفحه مدیریت حتماً از Endpoint جدید بالا استفاده شود.

### ۲. دریافت جزئیات

```http
GET /api/secretary/expense-categories/{id}
```

پاسخ موفق `200` شامل یک `ExpenseCategory` است. اگر رکورد وجود نداشته یا Soft Delete شده باشد، پاسخ `404` دریافت می‌شود.

### ۳. افزودن دسته‌بندی

```http
POST /api/secretary/expense-categories
```

Body:

```json
{
  "title": "تجهیزات",
  "isActive": true
}
```

پاسخ `data`:

```json
{
  "id": 13,
  "title": "تجهیزات",
  "isActive": true
}
```

- `title` اجباری و حداکثر ۱۰۰ کاراکتر است.
- عنوان قبل از ذخیره Trim می‌شود و نباید تکراری باشد.
- پیشنهاد می‌شود مقدار اولیه `isActive` در فرم `true` باشد.

### ۴. ویرایش دسته‌بندی

```http
PUT /api/secretary/expense-categories/{id}
```

Body:

```json
{
  "title": "تجهیزات و ابزار",
  "isActive": false
}
```

`id` فقط از URL خوانده می‌شود و نباید در Body ارسال شود. پاسخ موفق شامل مدل کامل و به‌روز دسته‌بندی است.

### ۵. حذف دسته‌بندی

```http
DELETE /api/secretary/expense-categories/{id}
```

حذف به‌صورت **Soft Delete** انجام می‌شود؛ دسته‌بندی غیرفعال شده و دیگر در فهرست‌ها نمایش داده نمی‌شود. پس از موفقیت، فرانت باید Modal را ببندد و فهرست را Refresh یا رکورد را از State حذف کند.

## رفتار صفحه و Modalها

### صفحه اصلی

- Header شامل عنوان «مدیریت دسته‌بندی‌های هزینه»، Breadcrumb حسابداری و دکمه افزودن باشد.
- Desktop: نمایش در Table با ستون‌های عنوان، وضعیت، تاریخ ایجاد، آخرین ویرایش و عملیات.
- Mobile: به‌جای فشرده‌کردن Table، هر دسته‌بندی به‌شکل Card/List Item نمایش داده شود.
- وضعیت با Badge قابل تشخیص «فعال» و «غیرفعال» نمایش داده شود و تنها به رنگ متکی نباشد.
- برای Loading از Skeleton، برای فهرست خالی از Empty State و برای خطا از Retry State استفاده شود.
- تاریخ‌های UTC با timezone و تقویم استاندارد پروژه نمایش داده شوند.

### Modal افزودن

- با دکمه «افزودن دسته‌بندی» باز شود.
- فیلد عنوان و Switch وضعیت فعال داشته باشد.
- عنوان Modal: «افزودن دسته‌بندی هزینه».
- دکمه اصلی هنگام ارسال Disable و دارای Loading باشد تا درخواست تکراری ارسال نشود.
- در موفقیت: بستن Modal، نمایش پیام API و Refresh فهرست.
- در خطا: Modal باز بماند و پیام API نمایش داده شود.

### Modal ویرایش

- با اکشن ویرایش هر ردیف/Card باز شود.
- داده فعلی می‌تواند از State فهرست پر شود؛ در صورت نیاز به داده تازه ابتدا API جزئیات فراخوانی شود.
- عنوان و وضعیت فعال قابل ویرایش باشند.
- عنوان Modal: «ویرایش دسته‌بندی هزینه».
- در موفقیت، رکورد فهرست با `data` پاسخ جایگزین شود.

### Modal حذف

- Modal تأیید مستقل با لحن هشدار نمایش داده شود.
- نام دسته‌بندی در متن تأیید ذکر شود: «آیا از حذف «مواد مصرفی» مطمئن هستید؟»
- دکمه خطر با عنوان «حذف دسته‌بندی» و دکمه انصراف وجود داشته باشد.
- هنگام درخواست، همه اکشن‌های Modal Disable شوند.
- حذف نباید Optimistic باشد؛ ابتدا پاسخ موفق API دریافت و سپس رکورد از UI حذف شود.

### نمایش جزئیات

- اکشن «مشاهده» باید `GET /{id}` را فراخوانی کند.
- عنوان، وضعیت، تاریخ ایجاد و تاریخ آخرین ویرایش نمایش داده شوند.
- برای `updatedAt: null` عبارت «ویرایش نشده» نمایش داده شود.

## الزامات Responsive و دسترس‌پذیری

- صفحه و تمام Modalها باید در Desktop، Tablet و Mobile کاملاً Responsive باشند.
- در Mobile، Modal به صورت Full-screen یا Bottom Sheet با عرض کامل و اکشن‌های Sticky نمایش داده شود.
- هیچ Horizontal Scroll اجباری برای عملیات اصلی ایجاد نشود.
- Touch targetها حداقل ۴۴×۴۴ پیکسل باشند.
- Modal باید Focus Trap، بستن با Escape، بازگرداندن Focus به Trigger و عنوان قابل دسترس داشته باشد.
- برای Inputها Label واقعی، پیام خطای متنی و `aria-describedby` فراهم شود.
- ترتیب Tab منطقی باشد و همه عملیات با Keyboard قابل انجام باشند.
- در RTL، چینش متن، Iconها، Modal و Breadcrumb به‌درستی بررسی شود.

## مدیریت State و خطا

- هنگام ورود به صفحه، فهرست یک‌بار Fetch شود.
- پس از Create/Delete می‌توان Refetch کرد؛ پس از Update بهتر است داده پاسخ مستقیماً در Cache جایگزین شود.
- خطای `401` باید وارد جریان Refresh Token/Login استاندارد پروژه شود.
- خطای `404` جزئیات یعنی رکورد حذف یا منقضی شده است؛ Modal بسته، پیام نشان داده و فهرست Refresh شود.
- خطای `400` شامل Validation، عنوان تکراری یا شناسه نامعتبر است و باید پیام API نمایش داده شود.
- در صورت خطای شبکه، داده فعلی صفحه حفظ و امکان Retry ارائه شود.

## چک‌لیست پذیرش فرانت‌اند

- [ ] گزینه مدیریت دسته‌بندی هزینه در منوی حسابداری اضافه شده است.
- [ ] دسترسی این بخش از Overview حسابداری وجود دارد.
- [ ] صفحه مستقل فهرست مدیریت پیاده‌سازی شده است.
- [ ] افزودن، ویرایش و حذف داخل Modal انجام می‌شوند.
- [ ] جزئیات هر دسته‌بندی قابل مشاهده است.
- [ ] دسته‌بندی‌های غیرفعال در صفحه مدیریت دیده می‌شوند.
- [ ] Loading، Empty، Error و Retry State وجود دارد.
- [ ] پیام‌های موفقیت و خطای API نمایش داده می‌شوند.
- [ ] ارسال تکراری فرم‌ها کنترل شده است.
- [ ] Desktop/Tablet/Mobile و RTL تست شده‌اند.
- [ ] Modalها با Keyboard و Screen Reader قابل استفاده‌اند.
- [ ] پس از هر Mutation، فهرست و Cache سازگار باقی می‌مانند.
